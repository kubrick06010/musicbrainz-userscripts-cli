# Collection exports produced by Get-MbCollection.ps1 (data, not source)
*.json

# Your real sync config (keep the .example in git, not local paths/collections)
collection_sync/collection_sync.config.ps1

# TagLibSharp auto-provisioned by the MusicBrainz module
musicbrainz/lib/
