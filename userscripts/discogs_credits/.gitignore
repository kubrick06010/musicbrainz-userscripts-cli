# Dependencies
node_modules/

# Playwright persistent profile (cookies, IDB cache — never commit)
.pw-profile/
storageState.json

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Build cache
.esbuild/
*.tsbuildinfo

# Logs from local runs
*.log
npm-debug.log*
pnpm-debug.log*

# Per-fixture import logs from `pnpm test`
test/logs/

# (Bot credentials + notif-pipeline state moved to the repo-root /.gitignore
# in <commit>; see /dev/ at the repo root.)
